// Re-export the hook for easy usage from anywhere
export { useReportModal } from "@/contexts/ReportContext";

// Re-export report types for type safety
export { REPORT_TYPE, REPORT_TYPE_LABELS } from "@/components/ReportModal/reportTypes";
